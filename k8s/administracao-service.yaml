apiVersion: v1
kind: Service
metadata:
  name: administracao-service
  namespace: producao
  labels:
    app: administracao
spec:
  selector:
    app: administracao
  ports:
    - name: http
      protocol: TCP
      port: 8086
      targetPort: 8086
      nodePort: 31000
  type: LoadBalancer
